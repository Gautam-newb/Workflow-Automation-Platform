[pytest]
# avoid scanning odd cache folders and common VCS/CI directories
norecursedirs = .git .github node_modules pytest-cache-files-*
# recommended options for CI/local runs
addopts = -q --maxfail=1 --disable-warnings --cache-clear
# use a local cache dir to avoid root-level permission issues
cache_dir = .pytest_cache

